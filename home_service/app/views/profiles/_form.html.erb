<%= form_with(model: profile, local: true) do |form| %>
<%#= form_with(model: profile) do |form| %>
<%#= form_with(url: user_profiles_path, local: true) do |form| %>
  <% if profile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(profile.errors.count, "error") %> prohibited this profile from being saved:</h2>

      <ul>
      <% profile.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="field">
     <%= form.label :phone %>
     <%= form.text_field :phone, id: :profile_phone %>
   </div>
   <div class="field">
      <%= form.label :street %>
      <%= form.text_field :street, id: :profile_street %>
   </div>
   <div class="field">
      <%= form.label :city %>
      <%= form.text_field :city, id: :profile_city %>
   </div>
   <div class="field">
      <%= form.label :state %>
      <%= form.text_field :state, id: :profile_state %>
   </div>
   <div class="field">
      <%= form.label :zipcode %>
      <%= form.text_field :zipcode, id: :profile_zipcode %>
   </div>
   <div class="field">
      <%= form.label :avatar %>
      <%= form.file_field :avatar, id: :profile_avatar %>
   </div>
   
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
