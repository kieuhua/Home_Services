
<%= form_with(url: category_subcategories_path, local: true) do |form| %> 
  <% if subcategory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(subcategory.errors.count, "error") %> prohibited this subcategory from being saved:</h2>

      <ul>
      <% subcategory.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :subname %>
    <%= form.text_field :subname, id: :subcategory_subname %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
